// Дан текст, содержащий целые числа. 
// Вывести на экран все имеющиеся в нем числа.

#include <iostream>
#include <string>

using namespace std;

int main()
{
    string s; unsigned int k = 0;
    getline(cin,s); // Ввод строки в стиле string
    while (k<s.length()) {
        if (isdigit(s[k])==0){ s.erase(k,1);}
            else k++;
    }
    cout << s << endl;
    return 0;
}
